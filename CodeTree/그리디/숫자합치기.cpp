#include <iostream>
#include <cstring>
#include <algorithm>
#include <vector>
#define f(a, b, c) for (int a = b; a < c; ++a)

int main(void) {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    
}
### 단위 정리  
- s : 문자열 
- $s_i$ : 문자열 왼쪽기준 i 번째의 문자  
- $sa[i]$ : 접미사 배열, (사전 순으로 이미 나열 됨)


### 참고 사이트

- [기본 내용 블로그](https://blog.naver.com/kks227/221028710658)  
- [그림으로 설명됨](https://m.blog.naver.com/jqkt15/222035128595)  
- [고급 기술 블로그](https://infossm.github.io/blog/2021/11/21/linear-suffix-array/)

# 접미사 배열  (Suffix Array)
접미사 : 문자열 $s$의 $i$ 번째 접미사 == $s$의 $i$번째 글자부터 마지막 글자까지 포함하는 부분문자열  

이를 집합시킨 것이 접미사 배열.  
여기서 접미사 배열에서 사전 순으로 오름차순으로 정렬함  

---
예시  
s = "banana"  
|$i$|sa[i]|
|---|------|
|0|banana|
|1|anana|
|2|nana|
|3|ana|
|4|na|
|5|a|  

여기서 $a[i] == $ 정렬 전 상태에서의 Suffix Array 인덱스값 으로 정의 

정렬할 경우

|$i$|$a[i]$|sa[i]|
|---|---|------|
|0|5|a|
|1|3|ana|
|2|1|anana|
|3|0|banana|
|4|4|na|
|5|2|nana|  
---

문자열로 저장하기엔 메모리 공간낭비가 심하므로 보통 $a[i]$ 정보만 저장함 

## 접미사 정렬 알고리즘 
Suffix Array을 "STL sort" 하고 lcp를 구하는 것은 매우느림. 시간복잡도 : $O(n^2logn)$ .  

따라서 좀 더 개선된 알고리즘이 필요함  

### $O(nlog^2n)$ 기법  
Manber-Myers 알고리즘 이라고도 함  

매번 정렬할 때, 그 기준을 계속해서 바꾸는 방법  
처음에는 첫글자 기준, 그 다음은 두 글자를 기준, 그 다음은 4번째 글자 , 8번째 글자, ... 이런 식으로 반복함  (2배수씩 증가)  

이 기준에 따라서 서로 같다고 판정되는 것들을 그룹핑시켜 번호를 매길 수 있음.

총 이 횟수가 $logn$ 이 되고, 기본 정렬 시간 복잡도가 $O(nlogn)$ 이므로, 토탈 $O(nlog^2n)$ 이 되는 것 

```c++

#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
const int MAX = 1<<19;
 
string S;
int N, d, sa[MAX], pos[MAX], temp[MAX] // pos: 그룹 번호, temp : 임시 그룹 번호 저장소
 
// 접미사 비교에 사용할 비교 함수
bool cmp(int i, int j){
    // 먼저 자신의 위치의 문자를 비교
    if(pos[i] != pos[j]) return pos[i] < pos[j];
    // 문자가 같으면 d칸 뒤의 문자끼리 비교
    i += d;
    j += d;
    return (i < N && j < N) ? (pos[i] < pos[j]) : (i > j);
}
 
// S를 사용해 sa 배열을 만드는 함수
void constructSA(){
    N = S.size();
    // 전처리
    for(int i=0; i<N; i++){
        sa[i] = i;
        pos[i] = S[i]; // 제일 첫 번째 루프에서는 제자리 문자로 비교
    }
    // d를 2배씩 늘려가면서 매번 앞에서부터 d*2글자만 보고 접미사 정렬
    for(d=1; ; d*=2){
        sort(sa, sa+N, cmp);
 
        // temp: 새로운 그룹 번호
        temp[MAX] = {0};
        // 앞에서부터 훑으면서 각 접미사가 서로 다른 그룹에 속할 때마다 그룹 번호 증가시킴
        for(int i=0; i<N-1; i++)
            temp[i+1] = temp[i] + cmp(sa[i], sa[i+1]);
        // pos 배열을 temp 배열로 대체
        for(int i=0; i<N; i++)
            pos[sa[i]] = temp[i];
 
        // 모든 접미사가 다른 그룹으로 나뉘어졌다면 종료
        if(temp[N-1] == N-1) break;
    }
}
 
int main(){
    scanf("%s", S);
    constructSA();
    for(int i=0; i<N; i++)
        printf("%d ", sa[i]+1);
}


// 여기서 comp를 이런식으로 lambda 함수로 정의도 가능함

auto cmp = [&d](int a, int b) {
    if (pos[a] != pos[b]) return pos[a] < pos[b];
    int a2 = a + d < N ? pos[a + d] : -1;
    int b2 = b + d < N ? pos[b + d] : -1;
    return a2 < b2;
};

```


### $O(nlogn)$ 기법  

일반적으론 위 $nlog^2n$ 만으로도 충분히 풀리지만 가끔 어떤 문제는 그러지 아니함.  
여기서 포인트는 정렬부분에서 $nlogn$ 에서 $n$ 으로 시간 복잡도를 줄여야 하는 것임  

사용할 수 있는 정렬법은 계수 정렬(Counting sort) 혹은 기수 정렬 (Radix sort) 가 있다.  

- [계수 정렬 포함한 SA 블로그](https://velog.io/@achui/%EB%AC%B8%EC%9E%90%EC%97%B4-Suffix-Array-LCP-1)

- [Counting sort for Stable Sort Pair Type](https://m.blog.naver.com/jqkt15/222031601969)
- [Counting sort for Stable Sort Pair Type 2 ](https://ravor.tistory.com/34)


여기서 중요한 것은 기수 정렬 할 때 queue를 사용할 경우 오히려 시간적 손해가 크다  


# LCP
Longest Common Prefix   
두 접미사의 최대 공통 접두사의 길이

**사용 전제 조건** : 정렬까지 완료된 Suffix Array  
($a[i]$, 정보와 이 $a[i]$를 사전순 정렬한 위치정보가 필요)

문자열을 하나만 주어 졌을 때 그 문자열의 부분문자열 중 공통된 부분문자열의 최대 길이가 얼마인지 파악하기에 좋은 알고리즘  



위에서 구한 정렬된 Suffix Array에서 $lcp[i]$ 값은  
**바로 $sa[i-1]$ 와 $sa[i]$의 겹치는 prefix** 값이 됨  
(첫 번째 Suffix 에 대해선 당연히 lcp 정보가 비게 됨 )  
(반드시 맨 쳐음부분 부터 탐색해서 공통 부분의 길이, 중간부터 같은건 인정 x)  



banana를 예시로 들 경우  (비교 : 현재와 직전방식)
|$i$|$a[i]$|$sa[i]$|$lcp[i]$|
|---|---|------|-----|
|0|5|a|x|
|1|3|ana|1|
|2|1|anana|3|
|3|0|banana|0|
|4|4|na|0|
|5|2|nana|2|  


다만 Naive하게 찾을려면 $O(n^2)$ 정도가 걸리기에 이는 비효율적임  
따라서 좀 더 개선된 방식이 필요함  

## 특징  
어떤 두 인접한 접미사 X, Y가 있고 그들의 LCP 값이 z(>0)면, X, Y에서 앞의 한 글자씩을 뺀 것 역시 접미사이며, 이들의 LCP 값은 최소 z-1이다.

결국 사전 순으로 정렬되어있는 SA는 이웃할 때 겹치는 부분이 많고, 이웃하지 않을 때는 겹치는 부분이
이웃할 때 보다는 무조건 작거나 같다는 의미이다. 


## 코드

```c++
// SA가 주어지면 LCP를 만드는 함수
void constructLCP(){
    // pos[i] = S[i:]가 sa의 몇 번째에 있는가 (pos[sa[i]] = i)
    // 제일 긴 접미사(S)부터 시작한다.
    // 매 루프마다 k>0이면 k--
    for(int i=0, k=0; i<N; i++, k=max(k-1, 0)){
        // 마지막 접미사(길이 1)면 아무것도 안 함
        if(pos[i] == N-1) continue;
 
        // 바로 아래 인접한 접미사와 비교하여 앞에서부터 몇 개의 글자가 일치하는지 센다
        for(int j=sa[pos[i]+1]; S[i+k]==S[j+k]; k++);
        lcp[pos[i]] = k;
    }
}
```
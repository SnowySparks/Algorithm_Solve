#include <string>
#include <vector>

using namespace std;
vector<pair<int, int> > gh[50001];

vector<int> solution(int n, vector<vector<int>> paths, vector<int> gates, vector<int> summits) {
    vector<int> answer;
    return answer;
}